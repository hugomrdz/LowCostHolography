# Essentials packages needes to run the programs, some dependecies may not be incluced in this file
# Also, these versions of packages are the ones used when getting the results, older versions may work
# but we don't guarantee that they will.

matplotlib==3.6.3
numpy==1.24.2
opencv-python==4.6.0.66
PyQt5==5.15.9
PyQt5-sip==12.11.1
pyqtgraph==0.13.1
PyQtWebEngine==5.15.6
rasterio==1.3.4

